# TechZen Smart Homes Web

Web profesional para TechZen Smart Homes, empresa de electricidad y domótica en Madrid.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run preview
npm run deploy
```

## Publicación en GitHub Pages

El proyecto está preparado para publicarse en:

```text
https://MI-USUARIO.github.io/techzen-smart-homes-web/
```

Opción recomendada:

1. Crear un repositorio público llamado `techzen-smart-homes-web`.
2. Subir el proyecto a la rama `main`.
3. Entrar en `Settings -> Pages`.
4. En `Build and deployment`, seleccionar `GitHub Actions`.
5. Ejecutar o esperar al workflow `Deploy to GitHub Pages`.

Opción alternativa con `gh-pages`:

1. Configurar el remoto GitHub.
2. Ejecutar `npm run deploy`.
3. En `Settings -> Pages`, seleccionar `Deploy from branch`.
4. Elegir la rama `gh-pages` y carpeta `/root`.

## Datos temporales a cambiar

- Telefono: `+34 675 734 122`
- WhatsApp: `34675734122`
- Email: `info@techzensmarthomes.com`
- Ciudad: `Madrid`
- Logo: `public/logo.svg`
- Dominio final cuando exista
